--CREATE FUNCTION PRECO_MEDIA(@NOME VARCHAR(30))
--RETURNS MONEY
--AS
--	BEGIN
--		DECLARE @MEDIA REAL
--		SELECT @MEDIA = (PRECO_LIVRO + PRECO_LIVRO + PRECO_LIVRO)/4.00
--		FROM TBL_LIVRO
--		WHERE NOME_LIVRO = @NOME
--		RETURN @MEDIA
--	END
	 
SELECT DBO.PRECO_MEDIA('SSH')